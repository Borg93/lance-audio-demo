{
  "//": "The vendored ra-anno annotation engine (src/lib/engine) is checked at ITS strictness — `strict: true`, but WITHOUT our two extra flags (noUncheckedIndexedAccess, exactOptionalPropertyTypes) that upstream wasn't written for. Keeps the engine in sync with upstream (no local rewrites) while our own code stays maximally strict. Run: bun run check:engine.",
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "noUncheckedIndexedAccess": false,
    "exactOptionalPropertyTypes": false
  },
  "include": ["src/lib/engine/**/*.ts"],
  "exclude": ["node_modules/**"]
}
