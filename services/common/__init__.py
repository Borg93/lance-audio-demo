"""Shared kernel for the lance-media services — lancekit, core, schemas, state, deps.

Mirrors lance-ns ``services/common``: every service imports shared primitives from
here and NEVER from a sibling service.
"""
